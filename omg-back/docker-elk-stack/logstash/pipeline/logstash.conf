input {
    kafka {
        bootstrap_servers => "kafka:9092"
        topics => ["OMG"]
        codec => json
        group_id => "logstash"
    }
}

filter {
    grok {
        match => { "message" => "%{TIMESTAMP_ISO8601:timestamp} \[%{GREEDYDATA:thread}\] %{LOGLEVEL:log_level} %{GREEDYDATA:logger} - %{GREEDYDATA:message}" }
    }
    date {
        match => [ "timestamp", "yyyy-MM-dd HH:mm:ss.SSS" ]
        target => "@timestamp"
    }
}

output {
    elasticsearch {
        hosts => ["elasticsearch:9200"]
        index => "logstash-%{+YYYY.MM.dd}"
        user => "${ELASTIC_USERNAME}"
        password => "${ELASTIC_PASSWORD}"
    }
}

# Elasticsearch 인덱스 템플릿
PUT _index_template/logs_template
{
  "index_patterns": ["logs-*"],
  "template": {
    "settings": {
      "number_of_shards": 1
    },
    "mappings": {
      "properties": {
        "@timestamp": { "type": "date" },
        "level": { "type": "keyword" },
        "service": { "type": "keyword" },
        "message": { "type": "text" }
      }
    }
  }
}

# Kibana 대시보드 예시 (JSON 형식)
{
  "title": "로그 분석 대시보드",
  "panels": [
    {
      "type": "visualization",
      "title": "로그 레벨 분포",
      "visState": "{\"type\":\"pie\",\"params\":{\"isDonut\":false},\"aggs\":[{\"id\":\"1\",\"type\":\"count\",\"schema\":\"metric\"},{\"id\":\"2\",\"type\":\"terms\",\"schema\":\"segment\",\"params\":{\"field\":\"level.keyword\"}}]}"
    },
    {
      "type": "search",
      "title": "최근 에러 로그",
      "searchSourceJSON": "{\"query\":{\"match\":{\"level\":\"ERROR\"}},\"sort\":[{\"@timestamp\":{\"order\":\"desc\"}}]}"
    }
  ]
}